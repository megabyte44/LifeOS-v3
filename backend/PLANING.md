# LifeOS Backend — Implementation Plan

> **Tech Stack:** Spring Boot 4.0.3, Java 17, PostgreSQL, Flyway, Firebase Auth, Lombok  
> **Package:** `com.lifos.backend`  
> **Server Port:** 8000  
> **Total Endpoints:** ~55 across 14 domains  

---

## Architecture Overview

```
HTTP Request
    │
    ▼
┌─────────────────────────┐
│  Firebase Auth Filter    │  ← Verifies Bearer token, extracts uid
└──────────┬──────────────┘
           ▼
┌─────────────────────────┐
│  REST Controller         │  ← Handles HTTP methods, request/response shaping
│  (@RestController)       │
└──────────┬──────────────┘
           ▼
┌─────────────────────────┐
│  Service Layer           │  ← Business logic, validation, transformations
│  (@Service)              │
└──────────┬──────────────┘
           ▼
┌─────────────────────────┐
│  Repository Layer        │  ← Database queries (auto-generated by Spring Data JPA)
│  (JpaRepository)         │
└──────────┬──────────────┘
           ▼
┌─────────────────────────┐
│  PostgreSQL Database     │  ← Tables created by Flyway migrations
└─────────────────────────┘
```

### Package Structure
```
com.lifos.backend/
├── LifeOsBackendApplication.java
├── config/           # CORS, Security, Firebase config
├── security/         # Firebase auth filter, SecurityContext helpers
├── entity/           # JPA entities (map to DB tables)
├── repository/       # Spring Data JPA interfaces
├── service/          # Business logic
├── controller/       # REST endpoints
├── dto/              # Request/Response DTOs
└── exception/        # Custom exceptions + global handler
```

---

## Phase 0: Project Configuration ✅ CURRENT
**Topics:** application.yaml, Flyway migrations, PostgreSQL setup, Firebase Admin SDK

### Steps
- [x] Configure `application.yaml` — datasource, server port, JPA, Flyway
- [x] Add Firebase Admin SDK dependency to `pom.xml`
- [x] Create Flyway migration `V1__create_users_table.sql`
- [x] Create Flyway migration `V2__create_todos_table.sql`

### Database: `lifeos`
```sql
-- Run in psql or pgAdmin to create the database:
CREATE DATABASE lifeos;
```

---

## Phase 1: First CRUD — Todos (Learn the Full Pattern)
**Topics:** Layered architecture, JPA Entity, JpaRepository, REST Controller, DTO, Lombok, UUID, CORS

### Steps
- [ ] Create CORS configuration (`WebConfig.java`)
- [ ] Create global exception handler (`GlobalExceptionHandler.java`)
- [ ] Create `User` entity + `UserRepository`
- [ ] Create `TodoItem` entity + DTO classes
- [ ] Create `TodoRepository`
- [ ] Create `TodoService` — CRUD logic
- [ ] Create `TodoController` — 4 endpoints
- [ ] Verify app compiles and starts
- [ ] Test all endpoints with Postman/curl (no auth yet)

### Endpoints
| Method | Path | Description |
|--------|------|-------------|
| GET | `/api/todos` | List all todos |
| POST | `/api/todos` | Create todo |
| PUT | `/api/todos/{id}` | Update todo |
| DELETE | `/api/todos/{id}` | Delete todo |

---

## Phase 2: Authentication — Firebase Security Filter
**Topics:** Firebase Admin SDK (Java), Spring Security Filter Chain, OncePerRequestFilter, SecurityContext

### Steps
- [ ] Add Firebase service account JSON to project
- [ ] Create `FirebaseConfig` — initialize Firebase Admin SDK
- [ ] Create `FirebaseAuthenticationFilter` — verify Bearer tokens
- [ ] Create `SecurityConfig` — configure filter chain
- [ ] Auto-create user on first login (if uid not in DB)
- [ ] Update all controllers to scope by authenticated user
- [ ] Test: login from frontend, verify API calls work

### Security Flow
```
Frontend (login) → Firebase Auth → Gets ID Token
Frontend (API call) → Authorization: Bearer <token> → Backend
Backend → FirebaseAuthFilter → Verify token → Extract uid → SecurityContext
Controller → Get uid from SecurityContext → Query user-scoped data
```

---

## Phase 3: Simple CRUDs — Replicate the Pattern
**Topics:** JSONB columns, Hibernate JSON type, @Enumerated, encryption at rest

### 3a. Users Endpoints
- [ ] `GET /api/users/me` — get current user profile
- [ ] `PATCH /api/users/me` — update profile

### 3b. Transactions
- [ ] Migration `V3__create_transactions_table.sql`
- [ ] Entity + DTO + Repo + Service + Controller
- [ ] Budget endpoints (`GET/PUT /api/transactions/budget`)

### 3c. Habits
- [ ] Migration `V4__create_habits_table.sql`
- [ ] JSONB `completions` field (Record<string, boolean|number>)
- [ ] `?context=gym` query parameter filter
- [ ] Entity + DTO + Repo + Service + Controller

### 3d. Notes
- [ ] Migration `V5__create_notes_table.sql`
- [ ] JSONB polymorphic `content` field
- [ ] Entity + DTO + Repo + Service + Controller

### 3e. Credentials (Password Vault)
- [ ] Migration `V6__create_credentials_table.sql`
- [ ] Encryption at rest for sensitive fields (passwords, PINs)
- [ ] Entity + DTO + Repo + Service + Controller

### 3f. Notifications
- [ ] Migration `V7__create_notifications_table.sql`
- [ ] `POST /mark-all-read` endpoint
- [ ] Entity + DTO + Repo + Service + Controller

### 3g. Preferences
- [ ] Migration `V8__create_preferences_table.sql`
- [ ] JSONB `features` and `onboarding` fields
- [ ] Entity + DTO + Repo + Service + Controller

---

## Phase 4: Complex Entities — Goals
**Topics:** @OneToMany, @ManyToOne, CascadeType.ALL, orphanRemoval, self-referencing entity, LAZY vs EAGER

### Steps
- [ ] Migration `V9__create_goals_tables.sql` (goals, sub_goals, progress_trackers, goal_notes, goal_resources)
- [ ] `Goal` entity with @OneToMany relationships
- [ ] `SubGoal` entity — self-referencing hierarchy
- [ ] `ProgressTracker`, `GoalNote`, `GoalResource` entities
- [ ] `GoalService` with nested CRUD
- [ ] `GoalController` — `GET/POST/PUT/DELETE /api/goals`, `GET /api/goals/{id}`

### Entity Relationships
```
Goal
 ├── ProgressTracker[]   (1:N, cascade ALL)
 ├── SubGoal[]           (1:N, cascade ALL, self-referencing tree)
 ├── GoalNote[]          (1:N, cascade ALL)
 ├── GoalResource[]      (1:N, cascade ALL)
 └── linkedHabitIds[]    (stored as JSONB string array)
```

---

## Phase 5: Planner & Gym Modules
**Topics:** Composite keys, multiple endpoints per domain, JSONB singletons

### 5a. Planner
- [ ] Migration `V10__create_planner_tables.sql`
- [ ] `GET /api/planner` — returns `Record<string, PlannerItem[]>`
- [ ] `PUT /api/planner/{day}` — replace day's items
- [ ] `POST/PUT/DELETE /api/planner/{day}/items/{id}`

### 5b. Gym Module (12 endpoints)
- [ ] Migration `V11__create_gym_tables.sql`
- [ ] Workout split — JSONB singleton (`GET/PUT`)
- [ ] Cycle config — single row (`GET/PUT`)
- [ ] Protein intakes — CRUD (`GET/POST/DELETE`)
- [ ] Food log — CRUD (`GET/POST/DELETE`)
- [ ] Completions — JSONB map (`GET/PUT`)
- [ ] Custom foods — JSONB array (`GET/PUT`)
- [ ] Protein target — single value (`GET/PUT`)

---

## Phase 6: Admin Module & Role-Based Access
**Topics:** Role-based authorization, @PreAuthorize, method-level security, global singletons

### Steps
- [ ] Migration `V12__create_admin_tables.sql` (ai_config, system_settings, announcements, about_page)
- [ ] `GET /api/admin/check` — admin status check
- [ ] `GET /api/admin/users` — list users with stats (cross-table join query)
- [ ] AI Configuration — `GET/PUT /api/admin/ai-config`
- [ ] System Settings — `GET/PUT /api/admin/settings`
- [ ] Announcements — full CRUD
- [ ] About Page — `GET/PUT /api/admin/about`
- [ ] `@PreAuthorize("hasRole('ADMIN')")` on all admin endpoints

---

## Phase 7: AI Chat & Push Notifications
**Topics:** HTTP client (RestTemplate/WebClient), proxying API calls, Web Push protocol, @Async

### 7a. AI Chat
- [ ] `POST /api/ai/chat` — proxy to configured AI provider
- [ ] Read API keys from `AiConfiguration` table
- [ ] Support OpenRouter, Gemini, OpenAI providers

### 7b. Push Notifications
- [ ] Migration `V13__create_push_subscriptions_table.sql`
- [ ] `POST /api/push/subscribe`
- [ ] `POST /api/push/unsubscribe`
- [ ] `POST /api/push/send-test`

---

## Phase 8: Testing & Polish
**Topics:** @SpringBootTest, @WebMvcTest, @DataJpaTest, MockMvc, Testcontainers, @ControllerAdvice

### Steps
- [ ] Unit tests for TodoService, GoalService
- [ ] Controller tests with MockMvc
- [ ] Repository tests with @DataJpaTest
- [ ] Integration tests for auth flow
- [ ] End-to-end: set `NEXT_PUBLIC_USE_MOCK_API=false` in frontend

---

## Database Tables Summary

| Table | Phase | Type |
|-------|-------|------|
| `users` | 0 | User-scoped root |
| `todos` | 0 | User-scoped CRUD |
| `transactions` | 3 | User-scoped CRUD |
| `budgets` | 3 | User-scoped singleton |
| `habits` | 3 | User-scoped CRUD (JSONB) |
| `notes` | 3 | User-scoped CRUD (JSONB) |
| `credentials` | 3 | User-scoped CRUD (encrypted) |
| `notifications` | 3 | User-scoped CRUD |
| `preferences` | 3 | User-scoped singleton (JSONB) |
| `goals` | 4 | User-scoped CRUD |
| `sub_goals` | 4 | Child of goals (self-ref) |
| `progress_trackers` | 4 | Child of goals |
| `goal_notes` | 4 | Child of goals |
| `goal_resources` | 4 | Child of goals |
| `planner_items` | 5 | User-scoped, keyed by day |
| `workout_splits` | 5 | User-scoped singleton (JSONB) |
| `cycle_configs` | 5 | User-scoped singleton |
| `protein_intakes` | 5 | User-scoped CRUD |
| `food_log` | 5 | User-scoped CRUD |
| `completed_workouts` | 5 | User-scoped singleton (JSONB) |
| `custom_foods` | 5 | User-scoped singleton (JSONB) |
| `protein_targets` | 5 | User-scoped singleton |
| `ai_configurations` | 6 | Global singleton |
| `system_settings` | 6 | Global singleton |
| `announcements` | 6 | Global CRUD |
| `about_page` | 6 | Global singleton |
| `push_subscriptions` | 7 | User-scoped |

---

## Key Decisions

1. **All IDs are UUIDs** — generated server-side, stored as `uuid` in Postgres
2. **JSONB for flexible fields** — habit completions, note content, workout splits, preferences
3. **Flyway for migrations** — explicit SQL, versioned, production-grade
4. **Firebase Auth** — verify tokens server-side, extract uid, auto-create user on first request
5. **Amounts in cents** — transactions store integers to avoid floating-point issues
6. **No auth in Phase 1** — build and test CRUD first, add auth in Phase 2, retrofit
7. **Credential encryption** — AES-256 for passwords/PINs stored at rest
