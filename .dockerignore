# Git
.git
.gitignore
.gitattributes
.github

# Build outputs
frontend/node_modules
frontend/.next
backend/target
backend/logs

# Env files — never bake secrets into the image
**/.env
!**/.env.example

# Codebase memory & docs
.codebase-memory
docs

# OS / IDE
.DS_Store
Thumbs.db
*.swp

# Firebase service account (must be provided via env var at runtime)
backend/src/main/resources/firebase-service-account.json
